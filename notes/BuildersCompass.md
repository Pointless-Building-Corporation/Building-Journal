# Open BuildersCompass.java

### `use()`
This should, in general, track the bounding boxes to define by the player. This is done via tags. Currently we only do cuboids.

The tag is stored in "FirstPos". It stores specifically the block adjacent to the looked at block (read block that can be placed on, generally).